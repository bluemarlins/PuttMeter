package com.bluemarlin.puttmeter.wearable.domain.model

/**
 * 단순화된 퍼팅 스트로크 모델
 * 
 * 이 파일은 단순화된 최대 속도 기반 측정을 위해 최소한의 데이터만 포함합니다.
 * 복잡한 5단계 감지는 제거되었으며, SimpleStrokeDetector에서 SimplePuttStroke를 사용합니다.
 */

// 이 파일은 하위 호환성을 위해 유지되지만, 실제로는 SimpleStrokeDetector.kt의
// SimplePuttStroke와 CalibrationData를 사용합니다.
